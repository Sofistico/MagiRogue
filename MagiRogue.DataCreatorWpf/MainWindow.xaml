<Window x:Class="MagiRogue.DataCreatorWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MagiRogue.DataCreatorWpf" xmlns:sys="clr-namespace:System;assembly=netstandard"
        Title="MainWindow" Height="450" Width="800"
        Background="{DynamicResource WindowBackgroundBrush}">

    <Window.Resources>
        <DataTemplate DataType="{x:Type local:ResearchViewModel}">
            <local:ResearchCU />
        </DataTemplate>
    </Window.Resources>
    <DockPanel>
        <Border DockPanel.Dock="Left" BorderBrush="Blue" BorderThickness="0,0,1,0">
            <StackPanel HorizontalAlignment="Center" Grid.Column="0">

                <ItemsControl ItemsSource="{Binding Pages}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding PageName}"
                        Margin="2,5" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <ContentControl DockPanel.Dock="Right" Content="{Binding CurrentPageView}" />
    </DockPanel>
</Window>
